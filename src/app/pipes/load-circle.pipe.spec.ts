import { LoadCirclePipe } from './load-circle.pipe';

describe('LoadCirclePipe', () => {
  it('create an instance', () => {
    const pipe = new LoadCirclePipe();
    expect(pipe).toBeTruthy();
  });
});
